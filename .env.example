# Lavpop Business Intelligence - Environment Variables
# 
# This file shows the required environment variables for local development.
# Copy this file to `.env` and fill in your actual values.

# Google Places API Key (for Google Business Widget)
# Get your key at: https://console.cloud.google.com/
# 1. Create a new project or select existing
# 2. Enable "Places API (New)" 
# 3. Create credentials → API key
# 4. Restrict the key:
#    - Application restrictions: HTTP referrers
#      - Add: localhost:5173/* (for local dev)
#      - Add: yourusername.github.io/* (for production)
#    - API restrictions: Select "Places API (New)"
VITE_GOOGLE_API_KEY=

# Note: For production deployment via GitHub Actions,
# add GOOGLE_API_KEY to your GitHub repository secrets
# (Settings → Secrets and variables → Actions → New repository secret)
